# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# HTTP
aiohttp>=3.9.0
httpx>=0.27.0

# WebSockets (AIS stream)
websockets>=12.0

# RSS parsing
feedparser>=6.0.11

# LLM agent — install for your chosen LLM_PROVIDER
anthropic>=0.28.0   # LLM_PROVIDER=claude
openai>=1.30.0      # LLM_PROVIDER=deepseek | openai | openrouter | local

# Data / analytics
pandas>=2.0.0
numpy>=1.26.0

# Satellite imagery (optional — for real Copernicus parsing)
# rasterio>=1.3.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
